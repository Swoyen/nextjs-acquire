import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.footer}>
      <span className={classes.title}>Made with ReactJS and NextJS © 2021</span>
    </footer>
  );
};

export default Footer;
